
// Generated by MyGeneration Version # (1.2.0.2)

using System;
using System.Data;
using System.Data.Common;
using System.Collections.Specialized;
using Microsoft.Practices.EnterpriseLibrary.Data;
using NCI.EasyObjects;

namespace NCI.EasyObjects.Tests.SQL
{
	public class CivilStatus : _CivilStatus
	{		
		public CivilStatus() {}

        // Place your custom code here.
        public void ResetTable()
        {
            string formatTruncate = "TRUNCATE TABLE [{0}].[{1}];";
            string formatDeleteAll = "DELETE FROM [{0}].[{1}];";
            string resetIdentity = " DBCC CHECKIDENT([{1}], RESEED, 0);";

            //  Create the Database object, using the default database service. The
            //  default database service is determined through configuration.
            Database db = GetDatabase();

            string sqlCommand = string.Format(formatTruncate + resetIdentity, this.SchemaTableView, this.TableName);
            DbCommand dbCommand = db.GetSqlStringCommand(sqlCommand);

            base.LoadFromSqlNoExec(dbCommand);
        }
    }
}
